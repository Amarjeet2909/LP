/* ============================================================
   CONFIGURATION TEMPLATE — safe to commit
   Copy this file to config.js and fill in your values.
   config.js is gitignored and will never be committed.
   ============================================================ */
var APP_CONFIG = {
  GOOGLE_SHEET_URL: ''   // paste your Google Apps Script Web App URL here
};
